# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller build output (run build.ps1 to regenerate dist\Subsync.exe)
build/
dist/
*.spec

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local dev / handoff notes — kept out of the public repo
CLAUDE.md
.claude/

# Release bundle built by release.ps1 (uploaded as an asset, not tracked)
Subsync.zip
